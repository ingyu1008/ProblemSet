#include <iostream>

int dp[10000];

int main(void)
{
    std::cin.tie(0);
    std::ios_base::sync_with_stdio(false);

    // int N, M;
    // std::cin >> N >> M;

    // int cnt = 0;

    // for (int i = 0, c, p, v; i < N; i++)
    // {
    //     std::cin >> p >> v;
    //     c = 1;
    //     while((c*(c+1)/2)*p <= M)
    // }

    return 0;
}